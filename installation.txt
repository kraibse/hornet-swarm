[general installation]
linux:
    sudo apt-get update
    sudo apt-get install nodejs npm

    npm install socket.io express


windows:
    get nodejs installer (and also select the npm option in it)

    npm install socket.io express